/**
 * Ambient module declarations for the LD editor extension.
 *
 * CSS imports are handled by the Theia esbuild bundler at build time;
 * TypeScript only needs a module declaration to compile.
 */

declare module '*.css';
