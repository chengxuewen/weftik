# OpenSpec Changes

变更提案目录。每个变更一个子目录，包含 proposal.md、design.md、specs/、tasks.md。

使用方法：
- `/openspec-propose <change-name>` — 创建变更提案
- `/openspec-apply` — 实施变更任务
- `/openspec-verify` — 验证变更
- `/openspec-archive` — 归档已完成变更
